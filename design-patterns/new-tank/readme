实现了一个创建一个弹框

继承了Frame类（设置 大小 标题 可移动 关闭属性）

重写了 paint方法 该方法的意思是：大概创建了一个黑色的方块



拓展====================
为社么java中wait() notify()方法不放在Threa类中呢？
 其中最主要的原因是java中的锁级别是对象，可以通过调用线程来获取锁notify和释放锁wait,如果把者两个方法放在Thread类中的话
 对象调用的话就不知道是该获取和释放哪个锁了